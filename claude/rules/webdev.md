---
paths:
  - "**/*.{tsx,jsx,vue,svelte,html}"
---

# web

- e2e and browser testing uses playwright, never selenium/cypress/puppeteer
- prefer role and label selectors over css/xpath
- no arbitrary waits, use web-first assertions
