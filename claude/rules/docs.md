---
paths:
  - "src/**/*"
  - "lib/**/*"
  - "app/**/*"
---

# docs

- if a change alters public API, config keys, env vars, or CLI flags, update the corresponding docs in the same commit
- if you can't find the doc that covers it, say so instead of guessing
- don't write docs that restate the code. document intent and gotchas
