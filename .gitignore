# macOS junk
.DS_Store
.AppleDouble
.LSOverride

# Editor / tool noise
*.swp
*.swo
*~
.vscode/
.idea/

# Logs & caches
*.log
.cache/
tmp/

# Secrets / local-only overrides
**/local.*
**/*.local
.env
.env.*

# WezTerm local overrides
wezterm/local.lua

# Shell local files
zsh/.zshrc.local

# Claude Code — config only, never state
.claude/settings.local.json
claude/**/state/
claude/**/*.doctor-backup

# SSH / GPG (never commit)
.ssh/
.gnupg/

# OS-specific
Thumbs.db
